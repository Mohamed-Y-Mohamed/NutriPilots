/**
 * Bumped whenever a function's observable behaviour changes.
 *
 * Deploying by hand means pasting five files into a dashboard, and a missed
 * paste is invisible: the app simply behaves like an older version. Returning
 * this from ai-chat and showing it in Settings turns "is it deployed?" from
 * guesswork into a fact you can read off the screen.
 */
export const FUNCTION_BUILD = "2026-08-10.3-suggestions+ingredients";
